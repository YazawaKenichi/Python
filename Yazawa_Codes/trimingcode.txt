"""		import		"""

import cv2

"""		priparation		"""
pass = input("加工したい画像のパスを入力してください。")
cv2.imread(pass)

"""		define		"""

def mode_output:
	print("1.縦横のピクセルを指定してトリミング")
	print("2.横のピクセルを指定してリサイズ")
	print("3.縦のピクセルを指定してリサイズ")
	print("4.縦横のピクセルを指定してリサイズ")
	print("5.容量を指定してリサイズ")
	print("6.容量を指定して正方形にトリミング")
	return 0

def select_mode:
	try:
		mode = int(input("モードを選んでください。>"))
	except:
		return 0
	return mode

def px_unit_trim:
	retry = 1
	while retry:
		center_point = center_setting()
		#入力された中心にバツ印を表記してウィンドウに出力する
	x = input("長さ >")
	y = input("高さ >")

"""			main		"""

while(1)
{
	mode_output()
	print("")
	mode = select_mode
	#mode の値によって実行する関数を変更する ifelif
	
}


